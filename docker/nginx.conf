server {
    listen 80;
    listen [::]:80;
    server_name _;

    root /var/www/html/webroot;
    index index.php;

    client_max_body_size 64M;

    # Logs
    access_log /var/log/nginx/planning_access.log;
    error_log  /var/log/nginx/planning_error.log warn;

    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    location ~ \.php$ {
        try_files $uri =404;
        include fastcgi_params;
        fastcgi_pass 127.0.0.1:9000;
        # $host retire le port ; $http_host le conserve (ex. 127.0.0.1:8080).
        # Sans ça, CakePHP redirige vers http://127.0.0.1/ et sort du mapping Docker.
        fastcgi_param HTTP_HOST $http_host;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
        fastcgi_read_timeout 300;
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 7d;
        access_log off;
        try_files $uri =404;
    }

    location ~ /\. {
        deny all;
    }
}
